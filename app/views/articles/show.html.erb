<h2>Name</h2>
<strong><%=@article.name%></strong>
<h2>Description</h2>
<strong><%=@article.description%></strong><br/>
<% @article.comments.each do |c|%>
<%= c.commenter%>
<%= c.body%><%= link_to "Edit", edit_article_comment_path(c.article,c) %>
|<%= link_to "Delete", article_comment_path(c.article,c),:method => :delete,:data=> {:confirm => "are u sure?"} %></td>
<br/>
<%end%>

<h2>Comments Form</h2>
<%= render "comments/form"	 %>
