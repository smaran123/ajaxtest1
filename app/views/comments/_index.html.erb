<% @article.comments.each do |c|%>
<div id = "comment-<%= comment.id%>">
<p>
<%= c.commenter%>
</p>
<p>
<%= c.body%><%= link_to "Edit", edit_article_comment_path(c.article,c) %>
|<%= link_to "Delete",article_comment_path(comment.article,comment) , :method => :delete, :remote => :true%>
</p>
</div>
<br/>
<%end%>
