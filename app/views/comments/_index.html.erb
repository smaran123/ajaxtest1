
<div id = "comment-index">
<% @article.comments.each do |c|%>
<div id = "comment-<%= c.id%>">
<p>
<%= c.commenter%>
</p>
<p>
<%= c.body%><%= link_to "Edit", edit_article_comment_path(c.article,c),:remote => :true %>|
|<%= link_to "Delete",article_comment_path(c.article,c) , :method => :delete, :remote => :true%>
</p>
</div>
<br/>
<%end%>
</div>